#include <stdio.h>

int main() {
	
    int A, B, X;
    
    // Leia os valores de A e B
    scanf("%d %d", &A, &B);

    // Calcule a soma de A e B
    X = A + B;

    // Imprima o resultado
    printf("X = %d\n", X);

    return 0;
}